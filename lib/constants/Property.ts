import { TableProps } from "@nextui-org/react";

export const TableProperties: TableProps = {
    color: "default",
    selectionMode: "multiple",
    hideHeader: false,
    isHeaderSticky: true,
    isStriped: false,
    isCompact: false,
};

export const TableBodyProperties = {
    emptyContent: "No data available...",
};
